name: DAST Staging

permissions:
  contents: read
  security-events: write
  pull-requests: read

on:
  workflow_dispatch:

jobs:
  dast:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Run OWASP ZAP or other DAST scan"
