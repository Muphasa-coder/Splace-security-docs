name: DAST Staging

permissions:
  contents: read
  security-events: write
  pull-requests: read

on:
  workflow_dispatch:

jobs:
  dast:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Run DAST scan
        run: echo "Run OWASP ZAP / other DAST scan here"
